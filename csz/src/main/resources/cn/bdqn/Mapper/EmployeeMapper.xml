<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--namespace:名称空间;为映射文件指定一个唯一的路径-->
<mapper namespace="cn.bdqn.Mapper.EmployeeMapper">
     <!--   创建自定义映射属性   -->
    <resultMap id="EmployeeResultMap" type="cn.bdqn.domain.Employee">
        <result property="empName" column="emp_name"/>
    </resultMap>
    <!--查询全部的员工。-->
    <select id="li"  resultMap="EmployeeResultMap">
        select id,emp_name,sex,email,birthday,address from t_employee
    </select>
    <!--   添加   -->
    <insert id="insert" parameterType="cn.bdqn.domain.Employee">
        insert into
        t_employee(emp_name,sex,email,birthday,address)
    values
        (#{empName},#{sex},#{email},#{birthday},#{address})
    </insert>
    <!--   根据id查询员工信息    -->
    <select id="selectAllEmployeesbyid" resultMap="EmployeeResultMap">
         select id,emp_name,sex,email,birthday,address from t_employee
         where id=#{id}
    </select>
   <!--  根据姓名模糊查询员工信息 -->
    <select id="selectByName" resultType="cn.bdqn.domain.Employee">
   	select
  		id,emp_name empName,sex,email,birthday,address
  	from
  		t_employee
  	where

  		emp_name like #{empName}
</select>
    <!-- 根据id删除员工信息  -->
    <delete id="daletebyid" parameterType="int" >
          delete from t_employee where id = #{id}
    </delete>
    <!-- 添加员工返回自增主键id   -->
    <insert id="insertReturnPrimaryKey_1" parameterType="cn.bdqn.domain.Employee">
        <selectKey keyProperty="id" keyColumn="id" order="AFTER" resultType="java.lang.Integer">
            select LAST_INSERT_ID()
        </selectKey>

        insert into
        t_employee(emp_name,sex,email,birthday,address)
        values
        (#{empName},#{sex},#{email},#{birthday},#{address})
    </insert>
    <!-- 根据id更新员工信息  -->
    <update id="updateidandname" parameterType="cn.bdqn.domain.Employee">
        update  t_employee
        set
         emp_name =  #{empName},
         address = #{address}
         where
            id = #{id}
    </update>
    <!-- 根据员工名和性别查询用户信息  -->
    <select id="nameandsex" resultMap="EmployeeResultMap">
          select
            id,emp_name empName,sex,email,birthday,address
        from
            t_employee
        where
            emp_name = #{empName} and sex = #{sex}
    </select>

</mapper>